VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server/admin API only. Never expose this to frontend code.
# Put real values in Vercel/local .env files, not in the repository.
SUPABASE_SERVICE_ROLE_KEY=
